schema { query: Query }

type Query { broken: Example }

type Example {
  value: Int! @resolve               # error: missing with
  total(limit: Int): Int! @load(with: { id: "limit" }) # error: @load on field with arguments
}
